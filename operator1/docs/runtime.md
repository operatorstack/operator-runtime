# Runtime

Runtime is execution resources and state passed into the engine.

Examples:

- `page`
- API client
- database handle

Runtime holds resources.

The engine performs execution with those resources.

Runtime is not task meaning and it is not capability selection.
