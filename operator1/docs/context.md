# Context

Context is structured execution input.

Examples:

- `url`
- `accountId`
- `invoiceId`
- `insurer`

Context is not:

- instructions
- hints
- prompt text

The engine reads context as concrete input for the run.
